/** Keys accepted by chart components and resolved by useChartColors(). */
export type ChartColorKey =
  | "ink" | "muted" | "positive" | "positiveLight" | "negative" | "negativeStrong"
  | "blue" | "purple" | "orange" | "ruleDash";
